# Copie para .env e ajuste conforme seu ambiente
# cp .env.example .env

PORT=3000

# MySQL (opcional — sem banco, o sistema usa arquivo local automaticamente)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=barbearia

# Forçar armazenamento em arquivo JSON (útil para demo)
# STORAGE=json
